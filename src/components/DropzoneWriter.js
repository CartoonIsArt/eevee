import React, { useState } from 'react'
import Dropzone from 'react-dropzone'

const Mention = require('antd/lib/mention')
const notification = require('antd/lib/notification')

const { toString, toContentState } = Mention

function DropzoneWriter(props) {
  const [contentState, setContentState] = useState(props.contentState)

  function 

  return (
  );
}

export default DropzoneWriter
